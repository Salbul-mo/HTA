package boj;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

import ex07_09_method_account_default.Numbers;

public class MagicStone2 {
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer stok = new StringTokenizer(br.readLine());
		int n = Integer.valueOf(stok.nextToken());
		int k = Integer.valueOf(stok.nextToken());
		Long i = Long.valueOf(stok.nextToken());
		Long limit = Math.
		bit[] arr = new bit[]
		
		for (int cnt = 0; cnt < ; cnt++) {
			System.out.println(Integer.toBinaryString(cnt));
		}
		
		
				
	}
	
	class 

}
